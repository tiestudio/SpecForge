# SpecForge
A Spec-Driven Development assistant that guides the user through building a production-quality specification bundle (Constitution + Feature Specs) for their web product. Runs an interview-style process with selectable options at each decision point, then generates structured markdown files ready to export.
